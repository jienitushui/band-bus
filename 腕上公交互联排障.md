# 腕上公交互联排障指南

本文用于处理手机端“腕上公交·定位中继”与手表端通信时出现的常见错误。

## 安装包位置

- 手机端 APK：`android/app/build/outputs/apk/debug/app-debug.apk`
- 手表端 RPK：`uno.keyin.bus/dist/uno.keyin.bus.debug.V26.2.1.BAND.rpk`

安装时应确保 APK 和 RPK 来自同一次项目构建。两端包名必须一致，当前均为：

```text
uno.keyin.bus
```

## 错误一：app not installed

示例：

```text
腕上公交·定位中继
主动定位推送失败：app not installed

腕上公交·定位中继
监听注册失败：app not installed
```

该错误表示小米 Wear 互联没有识别到与手机 App 对应的手表快应用。

常见原因：

- 手机端或手表端没有安装。
- 手机端安装的是新 APK，手表端仍是旧 RPK，或相反。
- APK 与 RPK 的包名不一致。
- APK 与 RPK 使用的签名证书不一致。
- 小米穿戴仍缓存旧的应用配对信息。

处理步骤：

1. 在手机上卸载旧版“腕上公交”。
2. 在手表或小米穿戴中卸载旧版“腕上公交”快应用。
3. 安装最新手机端 APK。
4. 安装最新手表端 RPK。
5. 重新打开小米穿戴，确认手表处于已连接状态。
6. 打开手机端“腕上公交”，进入右上角“设置”。
7. 检查“互联自检”结果，确认包名和签名证书均匹配。
8. 点击“刷新已连接设备”，确认页面显示手表节点 ID。

如果小米穿戴不允许覆盖安装 RPK，应先卸载旧快应用，再安装新 RPK。

## 错误二：fingerprint verify failed

示例：

```text
腕上公交·定位中继
主动定位推送失败：fingerprint verify failed
```

该错误表示小米 Wear 已识别到两端应用，但**手机 APK 与手表 RPK 的签名证书指纹不一致**。

常见原因：

- 手机装了新 APK，手表仍是旧证书打包的 RPK（或反过来）。
- 一端用工程 keystore（`xmswearable`），另一端用 IDE 默认 debug 证书。
- 小米穿戴缓存了旧的应用指纹配对信息。

处理步骤：

1. 卸载手机端「腕上公交」。
2. 在手表 / 小米穿戴中卸载手表端「腕上公交」快应用。
3. 用**同一次构建**安装最新 APK 与 RPK（本仓库 debug 均应对齐 `interconnect_dev_test_demo` 的 keystore + `uno.keyin.bus/sign/debug`）。
4. 打开小米穿戴，确认手表已连接。
5. 打开手机端腕上公交 → 设置 → 确认「互联自检」通过。
6. 刷新已连接设备 → 申请穿戴权限。
7. 重新启动定位中继，再打开手表快应用。

说明：定位中继在收到该错误后会**暂停主动定位推送**，避免通知栏反复刷失败；两端证书对齐并点设置里同步/推送城市配置后会恢复尝试。

## 错误三：permission denied

示例：

```text
腕上公交·定位中继
主动定位推送失败：permission denied
```

该错误表示小米 Wear 已经识别到两端应用，但手机 App 尚未获得当前手表节点的穿戴通信权限。

这里缺少的不是 Android 定位权限，而是小米 Wear SDK 的以下权限：

- `DEVICE_MANAGER`
- `NOTIFY`

### 正常授权步骤

1. 保持手机与手表在小米穿戴中正常连接。
2. 打开手机端“腕上公交”。
3. 进入右上角“设置”。
4. 点击“刷新已连接设备”。
5. 确认页面显示有效的节点 ID。
6. 点击“申请穿戴权限”。
7. 在小米穿戴弹出的授权页面中允许设备管理和通知/消息通信。
8. 返回腕上公交，点击“同步手表”或重新启动定位中继。
9. 打开手表端腕上公交，确认可以接收城市配置和定位数据。

### 没有出现授权弹窗

如果以前拒绝过授权，小米穿戴可能不会再次自动弹窗。可以尝试：

1. 打开“小米穿戴”。
2. 进入当前穿戴设备。
3. 查找“第三方应用”“应用管理”或类似入口。
4. 找到“腕上公交”。
5. 允许设备管理、通知或消息通信权限。
6. 强制停止并重新打开小米穿戴和腕上公交。
7. 回到腕上公交设置页，再次刷新节点并申请权限。

### 完全重置授权

如果仍然提示 `permission denied`，按以下顺序处理：

1. 卸载手机端腕上公交。
2. 卸载手表端腕上公交。
3. 强制停止或重启小米穿戴。
4. 先安装最新 APK。
5. 再安装最新 RPK。
6. 打开小米穿戴并确认手表已连接。
7. 打开手机端腕上公交设置页。
8. 点击“刷新已连接设备”。
9. 点击“申请穿戴权限”并允许全部请求。
10. 授权成功后再启动定位中继和手表快应用。

## 推荐安装与授权顺序

为减少缓存、签名和权限问题，建议每次测试新包时采用以下顺序：

1. 安装或更新手机端 APK。
2. 安装或更新手表端 RPK。
3. 打开小米穿戴，确认设备已连接。
4. 打开手机端腕上公交。
5. 进入设置并运行互联自检。
6. 刷新已连接设备。
7. 申请穿戴权限。
8. 授予 Android 前台与后台定位权限。
9. 启动定位中继。
10. 最后打开手表端腕上公交。

## 判断错误所处阶段

| 错误信息 | 含义 | 优先检查 |
| --- | --- | --- |
| `app not installed` | Wear 未识别到对应应用 | 两端安装包、包名、签名、安装缓存 |
| `fingerprint verify failed` | 两端签名指纹不一致 | 同证书重装 APK+RPK、互联自检 |
| `permission denied` | 已识别应用但未授权 | `DEVICE_MANAGER`、`NOTIFY` |
| `未连接手表节点` | SDK 没有找到连接设备 | 小米穿戴连接、蓝牙、第三方应用权限 |
| `连接断开` | 消息通道当前不可用 | 小米穿戴后台状态、蓝牙、系统省电限制 |
| `定位失败` | 已建立通信但没有可用坐标 | Android 定位权限、GPS、后台定位权限 |

## 注意事项

- Android 系统定位权限和小米 Wear 穿戴权限是两套独立权限，必须分别授予。
- 手机端与手表端必须使用相同包名和匹配的签名证书。
- 重装任意一端后，穿戴授权可能需要重新申请。
- 小米穿戴被系统结束、蓝牙断开或后台受限时，定位中继可能无法向手表发送消息。
- 建议允许小米穿戴和腕上公交自启动，并将电池策略设置为不限制。
