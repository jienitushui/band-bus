# 手表端与手机端请求性能优化方案

## 实施状态（2026-08-30）

本轮已完成以下客户端优化：

- 手表首页站点与线路快照持久化，启动时先展示缓存再刷新；
- 手表首屏只解析首站 CMD 209，CMD 115 返回后立即渲染；
- CMD 104 改为仅对缺少实时字段的前两条线路后台补齐；
- 手表互联冷启动轮询上限从约 18.9 秒降低到约 3.15 秒；
- 手表其他站点线路改为首屏完成后延迟加载；
- 安卓定位缓存持久化，首页可复用 15 分钟内的坐标并后台校正；
- 安卓附近站点与线路预览增加 30 分钟启动快照；
- 安卓站点线路增加 15 秒新鲜缓存和 30 分钟弱网失败兜底；
- 安卓网络超时和重试范围收紧，两端增加接口耗时日志；
- 两端在页面销毁或请求代次变化后隔离旧请求结果。

暂未实施的长期项：自建公交聚合服务、安卓网络层迁移到 OkHttp，以及基于大规模真机样本的 P50/P95 自动统计。这些工作应在本轮真机数据确认后决定是否继续。

## 1. 背景与结论

当前请求体感慢的主要原因不是公交源站单个接口持续响应缓慢，而是客户端把定位、互联等待和多阶段接口请求串联在首屏加载链路中。

本地直连 `https://h5.mygolbs.com/ApiData.do` 的抽样结果如下：

| 接口 | 用途 | 抽样耗时 |
| --- | --- | ---: |
| CMD 106 | 附近站点 | 约 0.15 秒 |
| CMD 209 | 物理站台解析 | 约 0.11 秒 |
| CMD 115 | 站点线路 | 约 0.12 秒 |
| CMD 102 | 线路/站点搜索 | 约 0.12 秒 |

实际设备上的网络质量会影响结果，但当前代码中更明显的问题是：

1. 手机首页先等待高精度定位，无新鲜定位时可能等待 15 秒。
2. 手表冷启动最多轮询互联状态约 18.9 秒。
3. 手表首屏按照 `106 -> 4 个 209 -> 115 -> N 个 104 -> 渲染` 执行，请求数量多且存在瀑布依赖。
4. 手机端站点预览需要先请求 209，再为物理站台逐个请求 115。
5. 部分手机请求失败后会完整重试一次，极端情况下单项等待可能接近 50 秒。
6. 手表侧 Promise 超时不会真正取消底层 fetch，过期请求仍可能占用网络资源。

优化原则是：**缓存先显示、关键请求优先、实时数据渐进更新、旧请求及时取消。**

## 2. 性能目标

| 指标 | 目标 |
| --- | ---: |
| 有缓存时两端首屏可见 | 小于 500ms |
| 有可用定位缓存时首次新数据 | P95 小于 3 秒 |
| 无定位缓存的冷启动 | P95 小于 8 秒 |
| 首条线路显示前的请求数 | 不超过 3 个 |
| 手表首页单次更新总请求数 | 通常不超过 6 个 |
| 页面退出后的无效请求 | 0 个 |
| 弱网或更新失败 | 保留上次可用数据 |

P50、P95 等指标必须基于真机日志统计，不能只依赖开发电脑的网络测量。

## 3. 目标请求流程

### 3.1 当前手表首屏流程

```text
等待互联
  -> 请求手机定位或手表 GPS
  -> CMD 106
  -> 并发请求最多 4 个 CMD 209，并等待全部完成
  -> CMD 115
  -> 为每条线路请求 CMD 104，并等待全部完成
  -> 渲染首屏
  -> 后台请求其他站点的 CMD 115
```

### 3.2 优化后的流程

```text
读取缓存站点和线路
  -> 立即渲染并显示更新时间
  -> 后台并行准备互联、定位和数据更新
  -> CMD 106
  -> 仅为当前首站请求 CMD 209
  -> CMD 115
  -> 立即渲染新线路
  -> 仅在缺少实时数据或用户进入详情时按需请求 CMD 104
  -> 其他站点滚动到可见区域后再加载
```

首屏不再等待所有实时信息完成。任何后台更新失败都不应清空已有内容。

## 4. 优化措施

### 4.1 重构手表首屏请求

涉及文件：`uno.keyin.bus/src/pages/index/index.ux`

1. 只为当前首站请求 CMD 209，其他站点采用可见时加载。
2. CMD 115 返回后立即渲染线路，不等待 CMD 104。
3. CMD 115 已包含 `neartext`、`neartime` 或 `neardis` 时，不再请求 CMD 104。
4. CMD 104 仅在以下情况请求：
   - CMD 115 缺少有效到站信息；
   - 用户打开线路详情；
   - 用户主动刷新实时信息；
   - 切换方向后需要更新当前可见线路。
5. CMD 104 最大并发数设为 2，只更新当前可见线路。
6. 其他站点的线路预览在滚动到可见区域后加载，不在首屏批量预取。

预期结果：首条线路出现前由十几个请求降低为 3 个，即 CMD 106、首站 CMD 209 和首站 CMD 115。

### 4.2 分级定位策略

涉及文件：

- `android/app/src/main/java/uno/keyin/bus/location/PhoneLocationCache.kt`
- `android/app/src/main/java/uno/keyin/bus/location/PhoneLocationHelper.kt`
- `android/app/src/main/java/uno/keyin/bus/LocationRelayService.kt`
- `uno.keyin.bus/src/pages/index/index.ux`

定位按缓存年龄分级：

| 定位年龄 | 行为 |
| --- | --- |
| 0 至 2 分钟 | 直接使用，不阻塞界面；必要时后台刷新 |
| 2 至 15 分钟 | 使用缓存加载数据，同时后台获取新坐标 |
| 超过 15 分钟 | 先尝试平衡功耗定位；2.5 秒未返回则展示上次数据，高精度定位在后台继续 |
| 用户手动重新定位 | 明确等待高精度定位，并展示定位进度 |

实施要求：

1. 将手机定位缓存从当前的进程内缓存升级为持久化缓存。
2. 缓存内容至少包含经纬度、定位时间、写入时间、精度和来源。
3. 手机收到手表定位请求后，优先返回符合条件的缓存坐标，再异步刷新定位。
4. 避免主动定位预热和手表定位请求同时启动两个高精度任务。
5. 新坐标与旧坐标距离变化较小时，不重复刷新附近站点。
6. 新坐标明显变化时，再重新执行附近站点请求。

### 4.3 缩短手表互联等待

涉及文件：

- `uno.keyin.bus/src/pages/index/index.ux`
- `android/app/src/main/java/uno/keyin/bus/LocationRelayService.kt`
- `android/app/src/main/java/uno/keyin/bus/wear/XmsWearSdkBridge.kt`

实施要求：

1. 缓存页面展示不等待互联就绪。
2. 互联冷启动等待上限由约 18.9 秒降低至 2 至 3 秒。
3. 优先通过连接状态事件继续流程，轮询只作为兼容性兜底。
4. 互联尚未就绪时保留缓存内容，并展示“正在更新”状态。
5. 互联恢复后自动触发一次合并后的刷新，不重复启动并行刷新流程。
6. 若手机缓存坐标可用，定位消息应优先秒回，随后再推送更精确的新坐标。

### 4.4 建立统一请求层

手机端可在 `BusApiClient` 外增加 Repository，也可以逐步将现有实现改造成统一请求入口。手表端应增加统一的请求调度器，避免每个页面各自直接调用 fetch。

统一请求层应提供：

1. 请求参数规范化和请求键生成。
2. 相同请求合并，多个调用方共享同一个进行中的请求。
3. 最大并发限制：手表 2 个，手机 4 个。
4. 按优先级排队：首屏关键请求高于预取和后台实时更新。
5. 页面销毁、请求代次变化或城市切换时取消旧请求。
6. 内存缓存和持久化缓存。
7. 单次请求截止时间和整条业务链截止时间。
8. 统一错误分类、重试和耗时日志。

手机端建议使用单例 OkHttp 客户端，以获得连接池、统一超时、请求取消和更清晰的拦截器能力。若暂不引入 OkHttp，也必须确保现有 HTTP 实现能够统一复用连接并取消过期任务。

### 4.5 缓存策略

| 数据 | 建议缓存时间 | 说明 |
| --- | ---: | --- |
| CMD 106 附近站点 | 2 分钟 | 按城市和位置网格缓存 |
| CMD 209 物理站台 | 24 小时 | 基本属于静态数据 |
| CMD 115 站点线路 | 10 至 15 秒 | 含动态到站字段 |
| CMD 104 实时车辆 | 8 至 10 秒 | 仅缓存短时间并按需更新 |
| CMD 103 线路详情 | 24 小时 | 线路站点等静态信息可长期缓存 |
| 搜索结果 | 1 至 5 分钟 | 当前手机端已有 60 秒缓存，可适当延长 |

缓存键必须包含影响结果的关键参数，例如城市版本、站点名称、站台坐标、线路、方向和站序。

缓存采用 stale-while-revalidate：先返回未超过允许陈旧上限的旧数据，再在后台更新；更新成功后局部刷新界面。

### 4.6 超时、重试与取消

建议策略：

1. 单个公交接口请求的首屏截止时间为 3 至 5 秒。
2. 首屏业务链整体截止时间不超过 8 秒。
3. 只对连接中断、DNS 短暂失败、502、503 和 504 等临时错误重试一次。
4. 参数错误、业务失败和普通 4xx 不重试。
5. 重试前等待 150 至 300ms，并加入少量随机抖动。
6. 重试必须服从整体截止时间，不能在首轮超时后重新获得完整超时时间。
7. 手表 Promise 超时时必须同步取消或失效化底层请求。
8. 页面隐藏、城市切换、定位更新或新一代请求开始后，旧请求结果不得更新 UI。

### 4.7 渐进式界面状态

界面状态调整为：

1. 有缓存时立即显示内容，并显示“更新于 HH:mm”。
2. 后台定位或网络更新期间，旧内容保持可读和可操作。
3. CMD 106 返回后更新站点列表。
4. CMD 115 返回后立即更新首站线路。
5. CMD 104 返回后只更新对应线路的到站字段。
6. 更新失败时保留旧数据，显示轻量错误信息和重试入口。
7. 仅在完全没有缓存和可用数据时显示全屏加载或空状态。

## 5. 可观测性设计

每次首页、搜索、线路详情和实时刷新流程生成一个 `traceId`，两端统一记录：

- 页面进入时间；
- 缓存读取耗时和缓存年龄；
- 首屏缓存渲染时间；
- 互联就绪耗时；
- 定位耗时、定位来源、定位精度和是否命中缓存；
- 每个 CMD 的排队时间、网络耗时、解析耗时和状态码；
- 首个站点出现时间；
- 首条线路出现时间；
- 完整实时数据完成时间；
- 请求总数、去重数、缓存命中数、取消数、重试数和超时数。

建议日志格式：

```text
[bus-perf] trace=abc123 platform=watch stage=cmd115 queueMs=12 networkMs=320 parseMs=8 cache=miss result=ok
```

手表调试日志可以通过互联发送到手机汇总，避免只能依赖手表屏幕上的临时文本。

## 6. 分阶段实施计划

### 阶段 0：建立基线

1. 增加两端分阶段耗时日志和 traceId。
2. 在 Wi-Fi、移动网络、弱网、手机锁屏和互联冷启动条件下采集基线。
3. 统计 P50、P95、请求数和失败率。

验收：能够明确判断一次慢请求卡在互联、定位、排队、网络还是解析阶段。

### 阶段 1：首屏快速收益

1. 移除手表首页对全部 CMD 104 的阻塞等待。
2. CMD 115 返回后立即展示线路。
3. 首屏只请求首站 CMD 209。
4. 使用已有缓存立即渲染，后台更新失败不清空旧数据。
5. 将手表互联等待上限降低至 2 至 3 秒。

验收：首条线路前最多 3 个公交接口请求，有缓存时 500ms 内出现内容。

### 阶段 2：定位与请求治理

1. 增加手机持久化定位缓存。
2. 实施分级定位策略和定位请求合并。
3. 建立两端统一请求调度、去重、缓存和取消机制。
4. 将其他站点和实时数据改为可见时加载。

验收：冷启动 P95 小于 8 秒；页面退出后没有无效请求继续更新 UI。

### 阶段 3：网络层和长期稳定性

1. 手机端迁移到单例 OkHttp 或完善现有连接复用。
2. 统一超时、重试、错误分类和整体截止时间。
3. 根据真机指标调整缓存时间和并发限制。
4. 评估是否需要增加自有聚合服务，将多级公交请求聚合成一个客户端接口。

聚合服务属于可选的长期方案。应先完成客户端减请求和渐进渲染，再根据真机数据决定是否引入额外服务成本。

## 7. 测试矩阵

至少覆盖以下组合：

| 场景 | 重点验证 |
| --- | --- |
| 手机和手表均为热启动 | 缓存首屏、无重复刷新 |
| 两端冷启动且互联正常 | 互联与定位耗时 |
| 手机锁屏、应用在后台 | 中继是否及时响应 |
| 互联尚未建立 | 缓存可用、等待上限有效 |
| 手机无新鲜定位 | 分级定位和超时回退 |
| 室内 GPS 弱信号 | 不长时间阻塞首屏 |
| Wi-Fi 正常网络 | 请求数和基准耗时 |
| 移动网络和模拟弱网 | 超时、重试、旧数据保留 |
| 快速切换城市或站点 | 旧请求取消和代次隔离 |
| 快速进出页面 | 页面销毁后无 UI 回写 |
| 公交接口业务失败 | 不错误重试、不清空缓存 |

## 8. 完成定义

只有同时满足以下条件，优化才视为完成：

1. 达到第 2 节的核心性能目标。
2. 请求数量有日志证据证明已经下降。
3. 弱网和接口失败时仍能展示上次可用数据。
4. 没有因为缓存造成城市、方向或站点数据串用。
5. 没有因为请求取消导致加载状态永久停留。
6. 手机锁屏和手表互联冷启动场景通过实机测试。
7. 搜索、换乘、线路详情和实时关注功能没有行为回归。
